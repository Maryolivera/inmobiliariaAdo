@model InmobiliariaAdo.Models.Inmueble
@{ ViewData["Title"] = "Eliminar Inmueble"; }

<h1>Eliminar Inmueble</h1>

<h3>¿Está seguro que desea eliminar este inmueble?</h3>

<div>
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Direccion)</dt>
        <dd class="col-sm-10">@Model.Direccion</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Uso)</dt>
        <dd class="col-sm-10">@Model.Uso</dd>

        <dt class="col-sm-2">Tipo</dt>
        <dd class="col-sm-10">@Model.TipoNombre</dd> <!-- 👈 cambio -->

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Ambientes)</dt>
        <dd class="col-sm-10">@Model.Ambientes</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Superficie)</dt>
        <dd class="col-sm-10">@Model.Superficie</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Precio)</dt>
        <dd class="col-sm-10">@Model.Precio.ToString("C")</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.PropietarioNombre)</dt>
        <dd class="col-sm-10">@Model.PropietarioNombre</dd>
    </dl>
</div>

<form asp-action="DeleteConfirmed" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Eliminar</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>
